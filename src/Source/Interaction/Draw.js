import Interaction from "../BaseClasses/Interaction";

export default class Draw extends Interaction{
  constructor(options){
    super(options)
  }
  init(){
    
  }
  destroy(){

  }
}
let draw=new Draw();